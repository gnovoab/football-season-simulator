<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

    <!--
        OWASP Dependency Check Suppressions for Football Season Simulator

        Add suppressions here for false positives or accepted risks.
        Each suppression should include:
        - notes: Explanation of why this is suppressed
        - cve or cpe: The vulnerability or package being suppressed
        - until: Optional expiration date for the suppression
    -->

    <!-- H2 Database - Development only, not exposed in production -->
    <suppress>
        <notes>
            H2 database is only used for development/testing with in-memory mode.
            Not exposed externally and not used in production.
        </notes>
        <packageUrl regex="true">^pkg:maven/com\.h2database/h2@.*$</packageUrl>
        <cpe>cpe:/a:h2database:h2</cpe>
    </suppress>

    <!-- Spring Boot DevTools - Development only -->
    <suppress>
        <notes>
            DevTools is only active in development mode and excluded from production builds.
        </notes>
        <packageUrl regex="true">^pkg:maven/org\.springframework\.boot/spring-boot-devtools@.*$</packageUrl>
        <vulnerabilityName regex="true">.*</vulnerabilityName>
    </suppress>

</suppressions>
